// auth.component.ts -> componente usato per l'autenticazione
import { Component } from '@angular/core';

@Component({
  selector: 'app-auth',
  templateUrl: './auth.component.html',
  styleUrls: ['./auth.component.css'],
})
export class AuthComponent {
  // Aggiungi la logica di gestione dell'autenticazione qui
}